
// This tool doesn't have a direct way to save the generated image to a specific path,
// but I can use an MCP tool if available, or just assume the user can see it.
// Wait, I CANNOT use write_to_file with image binary.
// I should use the image path from the previous tool.
// I'll use a shell command to copy it.
